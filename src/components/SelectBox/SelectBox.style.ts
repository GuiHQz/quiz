import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    number: {
        textAlign: "center",
        fontSize: "30px",
        padding: "40px 50px",
        marginTop: "17px",
        marginRight: "140px",
        backgroundColor: "#A970FF",
        cursor: "pointer",
    },

    flexArea: {
        display: "flex",
        flexDirection: "row",
    },
});